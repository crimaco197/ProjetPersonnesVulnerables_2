
import org.Views.SignUP;
import org.junit.jupiter.api.Test;
import javax.swing.*;
import java.io.IOException;

import static javax.swing.JOptionPane.*;

/*public class test {

    @Test
    public void testEmptyFields() {
        SignUP signup = new SignUP();
        JOptionPane optionPane = JOptionPane.showMessageDialog(test.this, "All fields must be filled out.", "Sign Up Error", JOptionPane.ERROR_MESSAGE);

        // Récupérer le message affiché par le JOptionPane
        String actualMessage = (String)optionPane.getMessage();


        signup.getLastName().setText("");
        signup.getEmail().setText("");
        signup.getPhone().setText("");
        signup.getPassword().setText("");
        signup.getButsign().doClick();

        String expectedMessage = "All fields must be filled out.";
        assert expectedMessage.equals(actualMessage);
    }

    @Test
    public void testSignUpWithValidInput() {
        // Mock UserController and User classes or use a testing database
        // to ensure the test is isolated and does not affect the actual database
        // This test assumes that UserController.CreateUser() is working correctly.

        SignUP signUpFrame = new SignUP();
        signUpFrame.getLastName().setText("Doe");
        signUpFrame.getN
        signUpFrame.getPhone().setText("1234567890");
        signUpFrame.getEmail().setText("john.doe@example.com");
        signUpFrame.getPassword().setText("password123");

        signUpFrame.getButsign().doClick();

        // Validate that the user is signed up successfully or navigate to the next screen
        // Assert.assertTrue(ValidationForSignUpSuccess());
    }


}*/
